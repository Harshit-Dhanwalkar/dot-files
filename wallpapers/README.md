# Wallpaper switcher script

[layer](https://github.com/Harshit-Dhanwalkar/layer.git) is own implementation of a wallpaper switcher in C, works with `swaybg` and `feh` to set wallpapers and preview them natively using default builtin `imageviewer` or any other image viewer like `sxiv` or `viu`.
