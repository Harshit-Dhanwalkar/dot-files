# Read `man 5 sway` for a complete reference.

### Variables
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP=Sway
# Mod4 for Logo key, use Mod1 for Alt.
set $mod Mod4
set $mod-alt Mod1
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l
set $term kitty
set $browser firefox
set $menu dmenu_path | dmenu | xargs swaymsg exec --

include /etc/sway/config-vars.d/*

# To change default terminal-emulator run `sudo update-alternatives --config x-terminal-emulator`
# set $term /usr/bin/kitty
set $term ~/.local/kitty.app/bin/kitty

### Output configuration
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
# output * bg /usr/share/backgrounds/sway/Sway_Wallpaper_Blue_1920x1080.png fill
output * bg ~/Pictures/wallpapers/Wall1.jpg fill

#   output HDMI-A-1 resolution 1920x1080 position 1920,0
# You can get the names of your outputs by running: swaymsg -t get_outputs

### Idle configuration
# exec swayidle -w \
#          timeout 300 'swaylock -f -c 000000' \
#          timeout 600 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"' \
#          before-sleep 'swaylock -f -c 000000'

### Input configuration
#   input "2:14:SynPS/2_Synaptics_TouchPad" {
input type:touchpad {
    tap enabled
    dwt enabled
    natural_scroll enabled
    middle_emulation enabled
}

# You can get the names of your inputs by running: swaymsg -t get_inputs
# Read `man 5 sway-input` for more information about this section.
### Key bindings
    # Start a terminal
    bindsym $mod+Return exec ~/.local/kitty.app/bin/kitty
    bindsym $mod-alt+Return exec foot
    # bindsym $mod+Shift+Return exec st

    # Lanch Browser
    bindsym $mod+Shift+f exec $browser

    # Kill focused window
    bindsym $mod+q kill

    # Screen Lock
    bindsym $mod-alt+Ctrl+l exec swaylock -f -c 1E1E2EFF
    # bindsym $mod-alt+Ctrl+l exec swaylock --image "$(find ~/Pictures/wallpapers -type f | shuf -n 1)" -C ~/.config/sway/swaylock/config
    # bindsym $mod-alt+Ctrl+l exec ~/.config/sway/scripts/swaylock-random.sh

    # Start application launcher
    # bindsym $mod+d exec $menu # this is default dmenu
    bindsym $mod+d exec /usr/bin/rofi -show drun -theme ~/.config/rofi/dracula.rasi
    bindsym $mod-alt+d exec dmenu_run

    # Quicklinks menu
    bindsym $mod-alt+t exec ~/.config/sway/scripts/dmenu-quicklink.sh
    # bindsym $mod+t exec ~/.config/sway/modules/QucikLink/quicklinks.sh # rofi

    # Start file namager
    bindsym $mod+m exec /usr/bin/nautilus

    # Start Disk Usage Analyzer
    bindsym $mod-alt+comma exec /usr/bin/baobab

    # Wifi/network
    bindsym $mod-alt+n exec ~/.config/sway/scripts/dmenu-wifi.sh

    # Swtich audio output
    # bindsym $mod-alt+a exec ~/.config/sway/scripts/audioswitch.sh
    bindsym $mod-alt+a exec  ~/.config/sway/scripts/controllers.sh

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod normal

    # Brightness Control
    # sudo visudo
    # add `user_name all=(all) nopasswd: /usr/bin/brightnessctl` at the end
    ## this will prevent `brightnessctl` cmd to use sudo everytime you use it.
    # sudo usermod -aG video harshitpd # logout, login again
    bindsym F6 exec sudo brightnessctl set 5%-
    bindsym F7 exec sudo brightnessctl set +5%
    # bindsym F6 exec /usr/bin/sudo /usr/bin/brightnessctl set 5%-
    # bindsym F7 exec /usr/bin/sudo /usr/bin/brightnessctl set +5%
    # bindsym F6 exec ~/.config/sway/scripts/dunst-slider.sh brightness_down
    # bindsym F7 exec ~/.config/sway/scripts/dunst-slider.sh brightness_up

    # # Special keys to adjust brightness via brightnessctl
    # bindsym --locked XF86MonBrightnessDown exec brightnessctl set 5%-
    # bindsym --locked XF86MonBrightnessUp exec brightnessctl set 5%+

    # Volume Control
    # bindsym F1 exec pactl set-sink-mute @DEFAULT_SINK@ toggle
    # bindsym F2 exec pactl set-sink-volume @DEFAULT_SINK@ -5%
    # bindsym F3 exec pactl set-sink-volume @DEFAULT_SINK@ +5%
    # bindsym F2 exec ~/.config/sway/scripts/dunst-slider.sh volume_down
    # bindsym F3 exec ~/.config/sway/scripts/dunst-slider.sh volume_up

    # # Special keys to adjust volume via PulseAudio
    bindsym --locked XF86AudioMute exec pactl set-sink-mute \@DEFAULT_SINK@ toggle
    bindsym --locked XF86AudioLowerVolume exec pactl set-sink-volume \@DEFAULT_SINK@ -5%
    bindsym --locked XF86AudioRaiseVolume exec pactl set-sink-volume \@DEFAULT_SINK@ +5%
    bindsym --locked XF86AudioMicMute exec pactl set-source-mute \@DEFAULT_SOURCE@ toggle

    # Screenshot
    bindsym Print exec grim -t png "$HOME/Pictures/Screenshots/screenshot_$(date +%Y-%m-%d_%H-%M-%S).png"
    bindsym $mod+s exec grim -g "$(slurp)" "$HOME/Pictures/Screenshots/screenshot_$(date +%Y-%m-%d_%H-%M-%S).png"
    bindsym $mod+shift+s exec grim -g "$(slurp)" - | wl-copy
    ## Uses demnu to bring out menu for screen shoot
    bindsym $mod-alt+s exec ~/.config/sway/scripts/screenshot-dmenu.sh

    # Browser
    # bindsym Mod1+w exec ~/.config/qutebrowser/userscripts/session.sh

    # Powermenu
    bindsym $mod+p exec ~/.config/sway/scripts/dmenu-power.sh

    # Reload the configuration file
    bindsym $mod+Shift+c reload

    # Exit sway (logs out of Wayland session)
    bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'

# Moving around:
    # Move your focus around
    bindsym $mod+$left focus left
    bindsym $mod+$down focus down
    bindsym $mod+$up focus up
    bindsym $mod+$right focus right
    # Or use $mod+[up|down|left|right]
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # Move the focused window with the same, but add Shift
    bindsym $mod+Shift+$left move left
    bindsym $mod+Shift+$down move down
    bindsym $mod+Shift+$up move up
    bindsym $mod+Shift+$right move right
    # with arrow keys
    bindsym $mod+Shift+Left move left
    bindsym $mod+Shift+Down move down
    bindsym $mod+Shift+Up move up
    bindsym $mod+Shift+Right move right

# Workspaces:
    # Switch to workspace
    bindsym $mod+1 workspace number 1
    bindsym $mod+2 workspace number 2
    bindsym $mod+3 workspace number 3
    bindsym $mod+4 workspace number 4
    bindsym $mod+5 workspace number 5
    bindsym $mod+6 workspace number 6
    bindsym $mod+7 workspace number 7
    bindsym $mod+8 workspace number 8
    bindsym $mod+9 workspace number 9
    bindsym $mod+0 workspace number 10
    # Move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace number 1
    bindsym $mod+Shift+2 move container to workspace number 2
    bindsym $mod+Shift+3 move container to workspace number 3
    bindsym $mod+Shift+4 move container to workspace number 4
    bindsym $mod+Shift+5 move container to workspace number 5
    bindsym $mod+Shift+6 move container to workspace number 6
    bindsym $mod+Shift+7 move container to workspace number 7
    bindsym $mod+Shift+8 move container to workspace number 8
    bindsym $mod+Shift+9 move container to workspace number 9
    bindsym $mod+Shift+0 move container to workspace number 10
    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.

# Layout stuff:
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym $mod+b splith
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    # bindsym $mod+s layout stacking
    # bindsym $mod+w layout tabbed
    # bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+space focus mode_toggle

    # Move focus to the parent container
    bindsym $mod+a focus parent

# Scratchpad:
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    bindsym $mod+Shift+minus move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    bindsym $mod+minus scratchpad show

# Resizing containers:
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# Status Bar:
bar {
    # waybar
        swaybar_command waybar
    # Default sway-bar
    # # Read `man 5 sway-bar` for more information about this section.
        position top
        status_command while date +'%Y-%m-%d %I:%M:%S %p'; do sleep 1; done
        separator_symbol "::"
        colors {
          background #222222
          statusline #9b8d7f
          separator #9b8d7f

          focused_workspace #9b8d7f #9b8d7f #222222
          inactive_workspace #222222 #222222 #9b8d7f
          active_workspace #222222 #222222 #9b8d7f
          urgent_workspace #222222 #222222 #9b8d7f
          binding_mode #222222 #222222 #9b8d7f
        }
        # font pango:Iosevka Term 12
        # tray_output HDMI-A-0
}

# title bar
# title_align left
# titlebar_padding 1
# titlebar_border_thickness 1
for_window {
    [title="Picture-in-Picture"] floating enable
    # [title="bluetoothctl"] floating enable, resize set 500 200
    # [app_id="pavucontrol"] floating enable
    # [app_id="^.*"] title_format "%title"
    # [class="^.*"] title_format "%title"
    # [app_id="^.*"] border pixel 3
    # [app_id="focused"] opacity 0.9
    # [app_id="focused_inactive"] opacity 0.8
    # [title="game-*"] floating enable, resize set 1920 1080
    # [title="app-*"] floating enable, resize set 1920 1080
}

# Workspaces
workspace_layout default
focus_follows_mouse yes
focus_wrapping no
# assign [app_id="(?i).*kitty.*"] $ws1
# assign [app_id="(?i).*firefox.*"] $ws2
# assign [app_id="(?i).*telegram.*"] $ws5
# assign [class="(?i).*discord.*"] $ws5
# assign [app_id="(?i).*gimp.*"] $ws6
# assign [app_id="(?i).*inkscape.*"] $ws6
# assign [app_id="(?i).*office.*"] $ws7
# assign [app_id="(?i).*zathura.*"] $ws7
# assign [app_id="(?i).*gnome-disks.*"] $ws8
# assign [app_id="(?i).*obsproject.*"] $ws8
# assign [app_id="(?i).*nwg-look.*"] $ws8
# assign [app_id="(?i).*localsend.*"] $ws8
# assign [app_id="(?i).*qbittorrent.*"] $ws8
# workspace 5 gaps horizontal 100
# workspace 5 gaps vertical 30
# workspace 5 gaps inner 20

# Adjusting borders
default_border pixel 1
# default_border none
default_floating_border none

# Gaps
gaps top 0
gaps inner 4
gaps outer 4

# focus the latest spawned window
for_window [shell=".*"] focus
# set all floating windows to center
for_window [floating] move position center,sticky disable

# # window rules for common titles
# for_window [app_id=".*" title="(?i).*open.*"] floating enable, move position center, resize set 60ppt 70ppt
#
# window settings for specific apps and their windows
for_window [app_id="(?i).*pavucontrol|blueman-manager|xdg-desktop-portal-gtk.*"] floating enable, move position center, resize set 60ppt 70ppt
for_window [class="(?i).*xdg-desktop-portal-gtk.*"] floating enable, move position center, resize set 60ppt 70ppt

for_window [window_type="dialog"] floating enable, move position center
for_window [window_type="utility"] floating enable, move position center
for_window [window_type="popup_menu"] floating enable, move position center

# class                     boarder   backgrd    text    indicator child_border
client.focused             "#FF9E64" "#1E1E2E" "#ffffff" "#FF9E64" "#777777"
client.focused_inactive    "#000000" "#222222" "#cccccc" "#888888" "#333333"
client.unfocused           "#89b4fa" "#1E1E2E" "#999999" "#111111" "#444444"
client.urgent              "#bf616a" "#f38ba8" "#ffffff" "#ff6600" "#990000"
client.placeholder         "#222222" "#222222" "#999999" "#777777" "#444444"

# Notification
exec --no-startup-id dunst

# Cliphist (clipboard-manager)
## [Github releases](https://github.com/sentriz/cliphist/releases)
# ```bash
# mv v0.*-linux-amd64 cliphist
# chmod +x cliphist
# ./cliphist version
# mv cliphist ~/.local/bin/
# ```
exec --no-startup-id wl-paste --type text --watch cliphist store -max-items 40
exec --no-startup-id wl-paste --type image --watch cliphist store -max-items 40
bindsym $mod-alt+v exec cliphist list | dmenu -l 10 | cliphist decode | wl-copy

# Dashboard
## [Github](https://github.com/kennylevinsen/wldash)
# bindsym $mod+g exec /usr/local/bin/wldash

# Launch dmenu notes
bindsym $mod-alt++Shift+1 exec ~/.config/sway/scripts/dmenu-notes.sh

# Define any word with Dmenu and dunst notification
bindsym $mod-alt+Shift+2 exec ~/.config/sway/scripts/dmenu-define.sh

# Color picker
# bindsym Mod1+c exec grim -g "$(slurp -p)" -t ppm - | convert - -format '%[pixel:p{0,0}]' txt:- | awk -F'[(,)]' '/srgb/{printf "#%02x%02x%02x\n", $2, $3, $4}' | xargs -I{} notify-send "Picked Color" "{}"
# bindsym Mod1+i exec ~/.config/sway/scipts/color-picker.sh
bindsym $mod-alt+p exec grim -g "$(slurp -p)" -t ppm - | convert - -format '%[pixel:p{0,0}]' txt:- | wl-copy
bindsym $mod-alt+Shift+3 exec sh -c 'HEX_COLOR=$(slurp -p | grim -g - - | convert - txt: | awk \"NR==2 { print \$3 }\"); wl-copy "$HEX_COLOR"; dunstify -a "Color Picker" "Copied Color" "Color: <b>$HEX_COLOR</b>"'

exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec --no-startup-id /usr/libexec/xdg-desktop-portal -r
exec dbus-update-activation-environment --systemd --all
